package amyc.langserver

import amyc.utils.Position
import org.eclipse.lsp4j.{TextDocumentPositionParams => LSP4jParam}

class PositionHelper {

 /* def convertPos(params: LSP4jParam): Position = {
    return SourcePosition(params.getUri(), params.getPosition().getLine(), params.getPosition().getCharacter())
  }*/

  /*def findInTree(pos: Position): ??? = {
    
  }*/

}